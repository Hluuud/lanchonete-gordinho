# Sprint 8.1 — Refinamento da Experiência do Cliente Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar a área do cliente (autoatendimento) de "sistema de pedidos" para "site oficial de hamburgueria", trocando a identidade textual pelo mascote Gordinho, simplificando a navegação, e adicionando ações flutuantes (carrinho/WhatsApp) e um hero em vídeo — sem tocar backend, checkout, painel admin/cozinha ou realtime.

**Architecture:** Seis fases sequenciais e independentemente testáveis, cada uma terminando em commit próprio. Fase A cria a fundação (paleta laranja + assets do mascote sem fundo). Fases B–C reestruturam navegação e ações (removem a topbar, adicionam bolha do mascote no mobile, FABs de carrinho/WhatsApp, sidebar simplificada). Fase D liga o hero em vídeo. Fase E injeta o mascote em pontos de interação fora do checkout. Fase F é o sweep final de contraste/limpeza.

**Tech Stack:** Next.js (App Router) + React 19 + Tailwind v4 (tokens OKLCH em `styles/globals.css`) + `framer-motion` + `sharp` (scripts de asset, Node) + `vitest`.

## Global Constraints

- Não alterar: backend, banco de dados, Supabase, `services/`, `repositories/`, checkout (`app/checkout`, `/checkout`), impressão, painel administrativo, painel da cozinha, realtime.
- Nenhuma regra de negócio muda — só apresentação de `features/menu` (área do cliente) e `features/cart` (visual do carrinho, não a lógica de `cart-context.tsx`).
- `:root` em `styles/globals.css` é o tema da loja (ADR 0007/0010) — os blocos `.dark` (cozinha) e `.theme-admin` não são tocados nesta sprint.
- Cada fase termina com: build, typecheck (`pnpm tsc --noEmit` ou equivalente do projeto), lint, testes (`pnpm test`), e commit Conventional Commits — igual ao fluxo já em uso no projeto (ver `CLAUDE.md`).
- Assets gerados por script (`public/brand/mascote-*.png`) são comitados — o build da Vercel não roda scripts de asset (mesma convenção da ADR 0011).

---

## Fase A — Fundação: paleta laranja e recorte do mascote

### Task A1: Função pura de chroma-key (remoção de fundo branco)

**Files:**
- Create: `scripts/lib/chroma-key.mjs`
- Test: `scripts/lib/chroma-key.test.ts`

**Interfaces:**
- Produces: `distanceToWhite(r: number, g: number, b: number): number` — distância euclidiana de um pixel RGB até o branco puro.
- Produces: `alphaFromDistance(distance: number, innerThreshold?: number, outerThreshold?: number): number` — alfa 0–255 com pena (feather) linear entre os dois limiares.

- [ ] **Step 1: Escrever o teste que falha**

```ts
// scripts/lib/chroma-key.test.ts
import { describe, expect, it } from "vitest";

import { alphaFromDistance, distanceToWhite } from "./chroma-key.mjs";

describe("chroma-key", () => {
  it("distância zero para branco puro", () => {
    expect(distanceToWhite(255, 255, 255)).toBe(0);
  });

  it("distância máxima para preto puro", () => {
    expect(distanceToWhite(0, 0, 0)).toBeCloseTo(441.67, 1);
  });

  it("alfa zero dentro do limiar interno (fundo)", () => {
    expect(alphaFromDistance(5)).toBe(0);
    expect(alphaFromDistance(12)).toBe(0);
  });

  it("alfa máximo fora do limiar externo (conteúdo opaco)", () => {
    expect(alphaFromDistance(60)).toBe(255);
    expect(alphaFromDistance(200)).toBe(255);
  });

  it("interpola linearmente na faixa de pena (feather)", () => {
    expect(alphaFromDistance(36, 12, 60)).toBe(128);
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `pnpm vitest run scripts/lib/chroma-key.test.ts`
Expected: FAIL — `Cannot find module './chroma-key.mjs'`

- [ ] **Step 3: Implementar**

```js
// scripts/lib/chroma-key.mjs

/**
 * Distância euclidiana de uma cor RGB até o branco de referência do fundo
 * das artes do mascote (`Boneco.png`, `Versoes_boneco.png`).
 */
export function distanceToWhite(r, g, b) {
  const dr = 255 - r;
  const dg = 255 - g;
  const db = 255 - b;
  return Math.sqrt(dr * dr + dg * dg + db * db);
}

/**
 * Alfa (0-255) de um pixel a partir da distância até o branco: dentro de
 * `innerThreshold` é fundo puro (alfa 0); fora de `outerThreshold` é
 * conteúdo opaco (alfa 255); entre os dois, interpola linearmente (pena),
 * evitando uma borda serrilhada ao redor do personagem.
 */
export function alphaFromDistance(
  distance,
  innerThreshold = 12,
  outerThreshold = 60,
) {
  if (distance <= innerThreshold) return 0;
  if (distance >= outerThreshold) return 255;
  const t = (distance - innerThreshold) / (outerThreshold - innerThreshold);
  return Math.round(t * 255);
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `pnpm vitest run scripts/lib/chroma-key.test.ts`
Expected: PASS (5 testes)

- [ ] **Step 5: Commit**

```bash
git add scripts/lib/chroma-key.mjs scripts/lib/chroma-key.test.ts
git commit -m "feat(brand): funcao pura de chroma-key para recorte do mascote"
```

---

### Task A2: Script de recorte do mascote (`Boneco.png` → PNGs transparentes)

**Files:**
- Create: `scripts/cutout-mascot.mjs`

**Interfaces:**
- Consumes: `distanceToWhite`, `alphaFromDistance` de `scripts/lib/chroma-key.mjs` (Task A1).
- Produces: `public/brand/mascote-full.png` (corpo inteiro, fundo transparente, ~800px de largura), `public/brand/mascote-avatar.png` (recorte quadrado cabeça/ombros, 512×512, fundo transparente) — consumidos por `BrandLogo` (Task B1) e `MascotNavBubble` (Task B4).

- [ ] **Step 1: Escrever o script**

```js
// scripts/cutout-mascot.mjs
/**
 * Recorta o mascote "Gordinho" das artes de origem, removendo o fundo
 * branco por chroma-key (distância até o branco, com pena/feather nas
 * bordas) e gerando as variantes usadas na Sprint 8.1.
 *
 * Rodar sempre que a arte-fonte (`public/brand/Boneco.png`) mudar. Os
 * arquivos gerados são commitados: o build (Vercel) não roda este script.
 *
 * Uso: node scripts/cutout-mascot.mjs
 */
import { mkdir, writeFile } from "node:fs/promises";
import { dirname, join, resolve } from "node:path";
import { fileURLToPath } from "node:url";

import sharp from "sharp";

import { alphaFromDistance, distanceToWhite } from "./lib/chroma-key.mjs";

const root = resolve(dirname(fileURLToPath(import.meta.url)), "..");

/** Fundo branco uniforme das duas artes-fonte do mascote. */
const INNER_THRESHOLD = 12;
const OUTER_THRESHOLD = 60;

/**
 * Remove o fundo branco de um PNG RGB, devolvendo um buffer PNG RGBA com
 * alfa calculado por pixel. Preserva a cor original do pixel — só o canal
 * alfa muda — para não escurecer bordas translúcidas.
 */
async function removeWhiteBackground(inputPath) {
  const { data, info } = await sharp(inputPath)
    .ensureAlpha()
    .raw()
    .toBuffer({ resolveWithObject: true });

  const rgba = Buffer.from(data);
  for (let i = 0; i < rgba.length; i += 4) {
    const distance = distanceToWhite(rgba[i], rgba[i + 1], rgba[i + 2]);
    rgba[i + 3] = alphaFromDistance(
      distance,
      INNER_THRESHOLD,
      OUTER_THRESHOLD,
    );
  }

  return sharp(rgba, {
    raw: { width: info.width, height: info.height, channels: 4 },
  })
    .png({ compressionLevel: 9 })
    .toBuffer();
}

async function write(relativePath, buffer) {
  const target = join(root, relativePath);
  await mkdir(dirname(target), { recursive: true });
  await writeFile(target, buffer);
  console.log(`${relativePath} (${(buffer.length / 1024).toFixed(1)} KB)`);
}

async function main() {
  const cutout = await removeWhiteBackground(
    join(root, "public/brand/Boneco.png"),
  );

  // trim() acha o bounding box do conteúdo opaco a partir do canto superior
  // esquerdo (agora transparente) — sobra só a figura do Gordinho.
  const trimmedSharp = sharp(cutout).trim();
  const { width, height } = await trimmedSharp.metadata();
  const trimmedBuffer = await trimmedSharp.toBuffer();

  await write(
    "public/brand/mascote-full.png",
    await sharp(trimmedBuffer)
      .resize({ width: 800 })
      .png({ compressionLevel: 9 })
      .toBuffer(),
  );

  // Avatar: quadrado a partir do topo da figura já recortada — cabeça e
  // ombros cabem na largura inteira de um personagem em pé. Centralizado
  // horizontalmente para o caso (raro) de a figura ser mais larga que alta.
  const avatarSide = Math.min(width, height);
  const avatar = await sharp(trimmedBuffer)
    .extract({
      left: Math.round((width - avatarSide) / 2),
      top: 0,
      width: avatarSide,
      height: avatarSide,
    })
    .resize(512, 512)
    .png({ compressionLevel: 9 })
    .toBuffer();

  await write("public/brand/mascote-avatar.png", avatar);
}

await main();
```

- [ ] **Step 2: Rodar o script**

Run: `node scripts/cutout-mascot.mjs`
Expected: imprime `public/brand/mascote-full.png (XX.X KB)` e `public/brand/mascote-avatar.png (XX.X KB)`, ambos os arquivos passam a existir.

- [ ] **Step 3: Verificar visualmente**

Abrir `public/brand/mascote-avatar.png` e `public/brand/mascote-full.png` — confirmar fundo transparente (sem halo branco) e enquadramento (avatar mostra cabeça/ombros, full mostra o corpo inteiro). Se houver halo branco residual nas bordas, reduzir `OUTER_THRESHOLD` e rerodar; se estiver "comendo" partes do personagem (ex. o branco do avental), aumentar `INNER_THRESHOLD` e rerodar.

- [ ] **Step 4: Commit**

```bash
git add scripts/cutout-mascot.mjs public/brand/mascote-full.png public/brand/mascote-avatar.png
git commit -m "feat(brand): script de recorte do mascote e assets sem fundo"
```

---

### Task A3: ADR 0013 — paleta laranja do mascote

**Files:**
- Create: `docs/adr/0013-paleta-laranja-do-mascote.md`

- [ ] **Step 1: Escrever a ADR**

```markdown
# ADR 0013 — Paleta laranja do mascote

- **Status:** Aceito
- **Data:** 2026-08-06
- **Contexto da fase:** Sprint 8.1 (Refinamento da Experiência do Cliente)

## Contexto

A ADR 0010 definiu preto/creme com vermelho pontual (`--primary` vermelho,
`--accent` laranja da logo). O lojista pediu, na Sprint 8.1, que a cor
principal do sistema seja exatamente o laranja da identidade visual do
mascote "Gordinho" — o selo `public/brand/logo.png`, não o boneco em si
(que não tem laranja nenhum).

Amostragem de cor do selo (30k pixels aleatórios) encontrou três tons de
laranja: `#F28C28` (círculo interno, dominante), `#FFB84D` (anel externo,
mais claro), `#D96318` (faixa "GORDINHO", mais escuro).

Testado o contraste WCAG dos dois primeiros contra texto branco e contra o
texto escuro que já existia como `--accent-foreground` (`oklch(0.24 0.03
65)`, ~`#291c10`):

| Fundo     | vs. branco | vs. `#291c10` |
|-----------|-----------:|--------------:|
| `#F28C28` |  2.45:1 ❌ |    6.75:1 ✅   |
| `#FFB84D` |  1.72:1 ❌ |    9.63:1 ✅   |

Texto branco sobre qualquer um dos dois tons falha WCAG AA (mínimo 4.5:1
para texto normal, 3:1 para texto grande) — os dois laranjas do selo são
claros o bastante para exigir texto escuro por cima, não branco.

## Decisão

`--primary` (bloco `:root`) passa de `oklch(0.53 0.205 27)` (vermelho) para
`oklch(0.73 0.161 59)` (`#F28C28`). `--primary-foreground` passa de
`oklch(0.99 0.004 90)` (branco) para `oklch(0.24 0.03 65)` (o mesmo tom
escuro que já era `--accent-foreground`).

`--accent` passa de `oklch(0.79 0.165 68)` para `oklch(0.83 0.145 74)`
(`#FFB84D`). `--accent-foreground` **não muda** — já era escuro o
suficiente para o novo tom (contraste 9.63:1).

`--destructive` **não muda** — continua vermelho (`oklch(0.63 0.235 25)`).
Os tokens de superfície escura (`--surface-dark*`) e `themeColor`
(`lib/brand/tokens.json`) também não mudam: a marca continua preto/creme
com o laranja em papel de destaque (CTA, ativo, badge), não substituindo o
preto do hero/sidebar.

`lib/brand/tokens.json` (`colors.primary`, `colors.accent`) é atualizado em
paralelo, mantendo as duas fontes de verdade em sincronia manual (dívida já
registrada na ADR 0011).

## Justificativa

- **Mesmo mecanismo da ADR 0010.** `:root` já é o tema da loja — só o valor
  muda, nenhum componente é tocado (`Button`, `Badge` etc. leem os tokens
  semânticos).
- **Contraste medido, não assumido.** Testar os dois tons de laranja contra
  branco e contra o escuro existente evitou publicar um CTA com texto
  ilegível — um erro fácil de cometer trocando "vermelho com texto branco"
  por "laranja com texto branco" sem medir.
- **Resolve de brinde a pendência da ADR 0010.** Aquela ADR registrava
  `--destructive` e `--primary` quase colidindo (ambos vermelhos). Com
  `--primary` agora laranja, os dois ficam claramente distintos sem
  precisar tocar em `--destructive`.

## Consequências

- Portais que leem `:root` fora do escopo de tema (Toaster, overlays Radix)
  herdam o laranja — mesma consequência já registrada na ADR 0010, agora
  com uma cor diferente.
- Textos que assumiam `--primary-foreground` branco (se algum componente
  hard-coded texto branco sobre um elemento com `bg-primary` em vez de usar
  o token) precisam ser auditados na Fase F desta sprint.

## Revisão

Reavaliar caso o lojista formalize identidade visual com agência.
```

- [ ] **Step 2: Commit**

```bash
git add docs/adr/0013-paleta-laranja-do-mascote.md
git commit -m "docs: ADR 0013 - paleta laranja do mascote"
```

---

### Task A4: Aplicar a nova paleta

**Files:**
- Modify: `styles/globals.css:32-33` (`--primary`, `--primary-foreground` do bloco `:root`)
- Modify: `styles/globals.css:43` (`--accent` do bloco `:root`)
- Modify: `lib/brand/tokens.json:8-9` (`colors.primary`, `colors.accent`)

**Interfaces:**
- Consumes: valores decididos na Task A3 (ADR 0013).

- [ ] **Step 1: Editar `styles/globals.css`**

Trocar (dentro do bloco `:root`, não nos blocos `.dark`/`.theme-admin`):

```css
--primary: oklch(0.53 0.205 27);
--primary-foreground: oklch(0.99 0.004 90);
```

por:

```css
--primary: oklch(0.73 0.161 59);
--primary-foreground: oklch(0.24 0.03 65);
```

E trocar:

```css
--accent: oklch(0.79 0.165 68);
```

por:

```css
--accent: oklch(0.83 0.145 74);
```

(`--accent-foreground` na linha seguinte permanece `oklch(0.24 0.03 65)`, sem alteração.)

- [ ] **Step 2: Editar `lib/brand/tokens.json`**

Trocar:

```json
"colors": {
  "primary": "#c7181d",
  "accent": "#ffa323",
```

por:

```json
"colors": {
  "primary": "#f28c28",
  "accent": "#ffb84d",
```

(`surfaceDark`, `themeColor`, `backgroundLight` permanecem inalterados.)

- [ ] **Step 3: Rodar o servidor de dev e verificar visualmente**

Run: `pnpm dev`

Abrir a Home (`/`) e checar: botão "Peça Agora" da sidebar, botão "Ver Cardápio"/"Fazer Pedido" do hero, item de navegação ativo — todos devem estar no novo laranja com texto escuro legível (não branco).

- [ ] **Step 4: Validações do projeto**

Run: `pnpm build && pnpm test`
Expected: build e testes passam sem erro (nenhum teste hoje assume o hex antigo).

- [ ] **Step 5: Commit**

```bash
git add styles/globals.css lib/brand/tokens.json
git commit -m "feat(brand): repintar --primary/--accent para o laranja do mascote"
```

---

## Fase B — Identidade e navegação

### Task B1: `BrandLogo` — variante "mascote" sem texto

**Files:**
- Modify: `components/brand-logo.tsx`

**Interfaces:**
- Consumes: `public/brand/mascote-avatar.png` (Task A2).
- Produces: `BrandLogo({ variant: "mascote", ... })` — nova opção de `BrandAssetVariant`, usada pela Task B2.

- [ ] **Step 1: Adicionar a variante ao componente**

Em `components/brand-logo.tsx`, trocar o tipo da variante (linha 21) de:

```ts
type BrandLogoVariant = "seal" | "horizontal" | "mono";
```

por:

```ts
type BrandLogoVariant = "seal" | "horizontal" | "mono" | "mascote";
```

E, antes do `if (variant !== "seal")` (linha 41), adicionar o ramo do mascote:

```tsx
  if (variant === "mascote") {
    return (
      <Image
        src="/brand/mascote-avatar.png"
        alt="Gordinho, o mascote da Lanchonete do Gordinho"
        width={px}
        height={px}
        priority={priority}
        className={cn("rounded-full object-cover", className)}
      />
    );
  }

  if (variant !== "seal") {
```

- [ ] **Step 2: Commit**

```bash
git add components/brand-logo.tsx
git commit -m "feat(brand): variante mascote no BrandLogo"
```

---

### Task B2: Sidebar usa o mascote como identidade, sem texto

**Files:**
- Modify: `features/menu/components/store-sidebar.tsx:50-59`

**Interfaces:**
- Consumes: `BrandLogo({ variant: "mascote" })` (Task B1), `scrollToSection` (já importado).

- [ ] **Step 1: Trocar o bloco de identidade**

Em `store-sidebar.tsx`, trocar (linhas 50–59):

```tsx
        <div className="flex items-center gap-3">
          <BrandLogo size="lg" priority />
          <div className="min-w-0">
            <p className="font-display text-xl leading-tight tracking-wide uppercase">
              {tenantName}
            </p>
            <p className="truncate text-xs text-surface-dark-muted">{SLOGAN}</p>
          </div>
        </div>
```

por:

```tsx
        <button
          type="button"
          onClick={() => scrollToSection("home")}
          aria-label={`${tenantName} — voltar para a Home`}
          className="flex w-fit items-center rounded-full transition-transform hover:scale-105"
        >
          <BrandLogo variant="mascote" size="xl" priority />
        </button>
```

- [ ] **Step 2: Remover o import agora sem uso**

Remover a linha `import { SLOGAN } from "@/features/menu/contact-info";` (linha 10) se `SLOGAN` não for mais usado em nenhum outro ponto do arquivo — verificar com:

Run: `grep -n "SLOGAN" features/menu/components/store-sidebar.tsx`
Expected: nenhuma ocorrência restante.

- [ ] **Step 3: Verificar visualmente**

Run: `pnpm dev` — na sidebar desktop (`lg:+`), o mascote circular substitui logo+nome+slogan, clicável, rolando para `#home`.

- [ ] **Step 4: Commit**

```bash
git add features/menu/components/store-sidebar.tsx
git commit -m "feat(menu): identidade da sidebar passa a ser so o mascote"
```

---

### Task B3: Remover a `StoreTopbar`

**Files:**
- Delete: `features/menu/components/store-topbar.tsx`
- Modify: `features/menu/components/store-experience.tsx:16` (remover import), `:98-107` (remover uso)

**Interfaces:**
- Consumes: nada novo — a bolha de navegação mobile (Task B4) assume o papel de abrir `StoreMobileNav`.

- [ ] **Step 1: Remover o uso em `store-experience.tsx`**

Remover a linha de import (linha 16):

```ts
import { StoreTopbar } from "@/features/menu/components/store-topbar";
```

E remover o bloco (linhas 98–107):

```tsx
        <StoreTopbar
          tenantName={menu.tenant.name}
          avgPrepMinutes={avgPrepMinutes}
          sections={sections}
          isFiltering={isFiltering}
          query={rawQuery}
          onQueryChange={setRawQuery}
        />

```

- [ ] **Step 2: Apagar o arquivo**

```bash
rm features/menu/components/store-topbar.tsx
```

- [ ] **Step 3: Verificar que nada mais importa `StoreTopbar`**

Run: `grep -rn "StoreTopbar" --include="*.tsx" --include="*.ts" .`
Expected: nenhuma ocorrência.

- [ ] **Step 4: Verificar que o build não quebra**

Run: `pnpm build`
Expected: sucesso — `StoreOpenBadge` e o gatilho de menu mobile são reintroduzidos pelas Tasks B4/C1/C2 nesta mesma fase, então o hero fica sem indicador de "aberto/fechado" e sem gatilho de menu só entre esta task e a Task B4 (estado intermediário aceitável dentro da mesma fase, antes do commit final de Fase B).

- [ ] **Step 5: Commit**

```bash
git add -A features/menu/components/store-topbar.tsx features/menu/components/store-experience.tsx
git commit -m "feat(menu): remover a topbar fixa"
```

---

### Task B4: Bolha flutuante do mascote (mobile) — identidade + gatilho de navegação

**Files:**
- Create: `features/menu/components/mascot-nav-bubble.tsx`
- Modify: `features/menu/components/store-experience.tsx` (renderizar a bolha + mover o estado do Drawer mobile pra dentro do próprio componente)

**Interfaces:**
- Consumes: `StoreMobileNav` (já existe, mesma API que a `StoreTopbar` usava), `scrollToSection`.
- Produces: `<MascotNavBubble tenantName sections query onQueryChange />` — usado por `StoreExperience` (Task B4) e nenhum outro consumidor.

- [ ] **Step 1: Criar o componente**

```tsx
// features/menu/components/mascot-nav-bubble.tsx
"use client";

import { useState } from "react";
import Image from "next/image";

import { StoreMobileNav } from "@/features/menu/components/store-mobile-nav";
import { scrollToSection } from "@/features/menu/scroll-to-section";
import type { StoreSection } from "@/features/menu/virtual-sections";

/**
 * Identidade + gatilho de navegação do mobile: bolha fixa do Gordinho no
 * canto superior esquerdo. Substitui a `StoreTopbar` removida — no desktop
 * (`lg:+`) a sidebar fixa já cobre identidade e navegação, então esta bolha
 * só renderiza abaixo de `lg` (ver classe `lg:hidden`).
 *
 * Um toque curto abre o Drawer de navegação (`StoreMobileNav`, o mesmo que
 * a topbar abria); nenhuma ação de toque leva para `#home` diretamente,
 * porque o próprio Drawer já lista "Home" como primeiro item — duplicar o
 * destino do toque só confundiria qual ação o mascote executa.
 */
export function MascotNavBubble({
  tenantName,
  sections,
  query,
  onQueryChange,
}: {
  tenantName: string;
  sections: StoreSection[];
  query: string;
  onQueryChange: (query: string) => void;
}) {
  const [open, setOpen] = useState(false);

  return (
    <>
      <button
        type="button"
        onClick={() => setOpen(true)}
        aria-label="Abrir menu de navegação"
        className="fixed top-4 left-4 z-40 flex size-14 items-center justify-center rounded-full bg-surface-dark shadow-lg ring-2 ring-primary transition-transform active:scale-95 lg:hidden"
      >
        <Image
          src="/brand/mascote-avatar.png"
          alt="Gordinho"
          width={56}
          height={56}
          className="size-full rounded-full object-cover"
        />
      </button>

      <StoreMobileNav
        open={open}
        onOpenChange={setOpen}
        tenantName={tenantName}
        sections={sections}
        query={query}
        onQueryChange={onQueryChange}
      />
    </>
  );
}
```

- [ ] **Step 2: Renderizar no `StoreExperience`**

Em `features/menu/components/store-experience.tsx`, adicionar o import:

```ts
import { MascotNavBubble } from "@/features/menu/components/mascot-nav-bubble";
```

E renderizar logo antes do `return` do `<div className="flex flex-1 flex-col ...">` (ou seja, como primeiro filho, um irmão da sidebar — não um filho dela), garantindo que fica fixo relativo à viewport:

```tsx
  return (
    <div className="flex flex-1 flex-col lg:grid lg:grid-cols-[300px_minmax(0,1fr)] lg:items-start">
      <MascotNavBubble
        tenantName={menu.tenant.name}
        sections={sections}
        query={rawQuery}
        onQueryChange={setRawQuery}
      />

      <StoreSidebar
```

- [ ] **Step 3: Verificar visualmente**

Run: `pnpm dev`, abrir em viewport mobile (`<1024px`): bolha do Gordinho aparece fixa no canto superior esquerdo, toque abre o Drawer com a navegação. Em viewport desktop (`≥1024px`): bolha não aparece (sidebar já cobre navegação).

- [ ] **Step 4: Commit**

```bash
git add features/menu/components/mascot-nav-bubble.tsx features/menu/components/store-experience.tsx
git commit -m "feat(menu): bolha do mascote como identidade e menu mobile"
```

---

### Task B5: Sidebar simplificada — reagrupar navegação sob "Cardápio"

**Files:**
- Modify: `features/menu/nav.ts`
- Modify: `features/menu/virtual-sections.ts:8-13,30-45` (comentário e defs)

**Interfaces:**
- Consumes: `Product.badges.isFeatured` (já existe), preço promocional (`isOnPromotion`, já existe em `virtual-sections.ts`), combos (fonte a decidir na Task B5 — ver Step 1).
- Produces: `STORE_NAV_ITEMS` com só 4 itens de topo (`Home`, `Cardápio`, `Sobre Nós`, `Contato`); `buildStoreSections` passa a incluir Promoções e Combos como seções virtuais nested sob "Cardápio".

- [ ] **Step 1: Adicionar Promoções e Combos como seções virtuais**

Combos ainda não têm produtos reais associáveis por badge (comentário em `virtual-sections.ts:11-12` diz "Combos seguem de fora até serem vendáveis"). Verificar antes de codar:

Run: `grep -n "isCombo\|combo" types/domain.ts features/menu/store-info.ts`

Se não existir nenhum campo de produto que marque "é combo", **não** criar uma seção virtual de Combos vazia (quebraria a regra de honestidade documentada no comentário) — nesse caso, manter "Combos" fora da navegação nesta sprint e registrar a exceção no BACKLOG.md ("Combos só entra na navegação da Fase B quando existir dado real de combo"), navegando o item apenas se `StoreCombos` (a seção da página) já teria conteúdo. Se existir o campo, seguir o Step 2 normalmente para Combos também.

Para Promoções (que já tem dado real via `isOnPromotion`), em `virtual-sections.ts`, adicionar aos `VIRTUAL_SECTION_DEFS` (linha 30):

```ts
const VIRTUAL_SECTION_DEFS: {
  slug: string;
  title: string;
  matches: (product: Product) => boolean;
}[] = [
  {
    slug: "promocoes-cardapio",
    title: "Promoções",
    matches: (product) => isOnPromotion(product),
  },
  {
    slug: "destaques",
    title: "Destaques da Casa",
    matches: (product) => product.badges.isFeatured,
  },
  {
    slug: "novidades",
    title: "Novidades",
    matches: (product) => product.badges.isNew,
  },
];
```

(`isOnPromotion` já está definida mais abaixo no mesmo arquivo — mover sua definição para antes de `VIRTUAL_SECTION_DEFS`, ou referenciá-la via function declaration hoisting, que já funciona para `function isOnPromotion` porque `function` é hoisted; não é necessário mover nada.)

Atualizar o comentário do topo do arquivo (linhas 8-13) removendo a frase que agora está desatualizada ("'Mais Vendidos' e 'Promoções' não entram aqui"), já que Promoções passa a entrar. Deixar:

```ts
/**
 * Seções do cardápio na experiência de autoatendimento: as "virtuais"
 * (derivadas dos badges/preço existentes — nunca dados fabricados) seguidas
 * das categorias reais do banco.
 *
 * "Mais Vendidos" tem seção própria na página, fora do bloco do cardápio
 * (ver `selectBestsellers` abaixo) — não é uma seção virtual daqui porque
 * não é uma categoria de produtos, é um recorte de "melhores vendas" que já
 * tem sua própria vitrine (Sprint 7).
 */
```

Nota: o slug de Promoções aqui é `promocoes-cardapio`, não `promocoes` — a página já tem uma seção `id="promocoes"` própria (`StorePromoBanner`, fora do bloco do cardápio, ver Fase E). Usar o mesmo slug colidiria duas âncoras `#promocoes` na mesma página. O item de navegação apontará para `#promocoes` (a seção de página já existente, mais rica visualmente) e não para esta seção virtual — a seção virtual serve apenas para os produtos com preço promocional aparecerem también dentro da listagem do cardápio, quando existirem.

- [ ] **Step 2: Reescrever `STORE_NAV_ITEMS`**

Trocar todo o corpo de `buildStoreNavItems` em `features/menu/nav.ts` (linhas 51–81) por:

```ts
export function buildStoreNavItems({
  hasGallery = false,
  hasTestimonials = false,
}: BuildStoreNavItemsOptions = {}): StoreNavItem[] {
  return [
    { anchor: "home", label: "Home", icon: Home },
    {
      anchor: "cardapio",
      label: "Cardápio",
      icon: UtensilsCrossed,
      isHeading: true,
    },
    { anchor: "sobre", label: "Sobre Nós", icon: Info },
    { anchor: "contato", label: "Contato", icon: Phone },
  ];
}
```

Remover os imports de ícones que ficaram sem uso (`Images`, `MessageSquareQuote`, `Package`, `Sparkles`, `TrendingUp`) e os imports de `GALLERY_ITEMS`/`hasGalleryItems`/`TESTIMONIALS`/`hasTestimonialItems` — verificar com:

Run: `grep -n "hasGallery\|hasTestimonials\|GALLERY_ITEMS\|TESTIMONIALS" features/menu/nav.ts`

Se `hasGallery`/`hasTestimonials` não forem mais lidos em nenhum lugar do arquivo, remover também `BuildStoreNavItemsOptions` e simplificar `buildStoreNavItems()` para não receber parâmetros — e atualizar a chamada em `STORE_NAV_ITEMS` (linhas 87-90) para `buildStoreNavItems()`, sem argumentos. O JSDoc do tipo/função deve ser reescrito para não mencionar Galeria/Depoimentos.

- [ ] **Step 3: Verificar que a Sidebar/Drawer ainda nesteiam as categorias reais**

`store-sidebar.tsx`/`store-mobile-nav.tsx` já nesteiam `sections` (passadas via prop) sob o item `isHeading` — nenhuma mudança necessária nesses dois arquivos: eles continuam recebendo `sections = buildStoreSections(menu)`, que agora inclui a seção virtual "Promoções" (produtos com preço promocional) e "Destaques da Casa" já nested sob "Cardápio", junto das categorias reais (Hambúrgueres, Pastéis, Porções, Bebidas, Sobremesas, conforme o cadastro do lojista).

- [ ] **Step 4: Validações**

Run: `pnpm build && pnpm test`
Expected: sucesso. Se algum teste existente referenciar `Galeria`/`Depoimentos`/`Mais Vendidos` em `STORE_NAV_ITEMS`, atualizar a expectativa do teste para a nova lista de 4 itens.

- [ ] **Step 5: Verificar visualmente**

Run: `pnpm dev` — sidebar mostra: Home, Cardápio (expandido: Promoções [se houver produto em promoção], Destaques da Casa [se houver produto featured], categorias reais), Sobre Nós, Contato.

- [ ] **Step 6: Commit**

```bash
git add features/menu/nav.ts features/menu/virtual-sections.ts
git commit -m "feat(menu): navegacao simplificada - 4 itens, cardapio agrupa promocoes e destaques"
```

---

## Fase C — Ações flutuantes

### Task C1: FAB do carrinho

**Files:**
- Create: `features/cart/components/cart-fab.tsx`
- Modify: `features/menu/components/store-experience.tsx` (renderizar)

**Interfaces:**
- Consumes: `useCart()` (`totalQuantity`, `subtotalCents`, `setOpen`, já existentes em `features/cart/use-cart.ts`), `PriceTag` (`components/price-tag.tsx`, já usado em `cart-panel-content.tsx`).
- Produces: `<CartFab />` sem props — lê o carrinho direto do contexto, igual ao antigo botão da topbar.

- [ ] **Step 1: Criar o componente**

```tsx
// features/cart/components/cart-fab.tsx
"use client";

import { motion, useReducedMotion } from "framer-motion";
import { ShoppingBag } from "lucide-react";

import { PriceTag } from "@/components/price-tag";
import { useCart } from "@/features/cart/use-cart";

/**
 * Botão flutuante do carrinho (canto inferior direito), substituindo o
 * botão que morava na `StoreTopbar` removida na Fase B. Mostra quantidade e
 * valor total; pulsa só quando a quantidade muda (feedback de "Adicionar"),
 * nunca continuamente — não deve competir por atenção com o conteúdo.
 */
export function CartFab() {
  const { totalQuantity, subtotalCents, setOpen } = useCart();
  const prefersReducedMotion = useReducedMotion();

  if (totalQuantity === 0) return null;

  return (
    <motion.button
      type="button"
      onClick={() => setOpen(true)}
      aria-label={`Abrir carrinho, ${totalQuantity} ${totalQuantity === 1 ? "item" : "itens"}`}
      key={totalQuantity}
      initial={prefersReducedMotion ? false : { scale: 1.08 }}
      animate={{ scale: 1 }}
      transition={{ type: "spring", stiffness: 400, damping: 20 }}
      className="fixed right-4 bottom-4 z-40 flex items-center gap-2 rounded-full bg-primary px-5 py-3.5 text-primary-foreground shadow-lg active:scale-95"
    >
      <ShoppingBag className="size-5" aria-hidden />
      <span className="text-sm font-bold tabular-nums">{totalQuantity}</span>
      <span className="h-4 w-px bg-primary-foreground/30" aria-hidden />
      <PriceTag cents={subtotalCents} className="text-sm font-bold" />
    </motion.button>
  );
}
```

- [ ] **Step 2: Renderizar no `StoreExperience`**

Em `store-experience.tsx`, adicionar o import `import { CartFab } from "@/features/cart/components/cart-fab";` e renderizar `<CartFab />` como irmão da `<MascotNavBubble />` (fora da árvore da sidebar/conteúdo, para não herdar `overflow` de nenhum contêiner com scroll).

- [ ] **Step 3: Verificar visualmente**

Run: `pnpm dev` — carrinho vazio: nenhum FAB visível. Adicionar item: FAB aparece no canto inferior direito com quantidade+valor, pulsando na primeira aparição e em cada incremento.

- [ ] **Step 4: Commit**

```bash
git add features/cart/components/cart-fab.tsx features/menu/components/store-experience.tsx
git commit -m "feat(cart): botao flutuante do carrinho substitui o da topbar"
```

---

### Task C2: FAB do WhatsApp

**Files:**
- Create: `features/menu/components/whatsapp-fab.tsx`
- Modify: `features/menu/components/store-experience.tsx` (renderizar)

**Interfaces:**
- Consumes: `WHATSAPP_LINK` (`features/menu/contact-info.ts`, já existe), `WhatsAppIcon` (`features/menu/social-icons.tsx`, já existe).

- [ ] **Step 1: Criar o componente**

```tsx
// features/menu/components/whatsapp-fab.tsx
import { WHATSAPP_LINK } from "@/features/menu/contact-info";
import { WhatsAppIcon } from "@/features/menu/social-icons";

/**
 * Botão flutuante do WhatsApp, acima do `CartFab` no canto inferior
 * direito. Sem pulso constante — nunca deve competir visualmente com o
 * carrinho, que é a ação primária.
 */
export function WhatsappFab() {
  return (
    <a
      href={WHATSAPP_LINK}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Falar no WhatsApp"
      className="fixed right-4 bottom-20 z-40 flex size-12 items-center justify-center rounded-full bg-success text-white shadow-lg transition-transform active:scale-95"
    >
      <WhatsAppIcon className="size-6" />
    </a>
  );
}
```

Verificar antes se o token `bg-success` existe em `styles/globals.css` (já usado em `store-open-badge.tsx` como `bg-success/15 text-success`):

Run: `grep -n "\-\-success" styles/globals.css`

Se `--success` for só uma cor de texto/fundo suave (`/15`) e não tiver um tom sólido pensado para preencher um botão inteiro, usar o hex oficial do WhatsApp (`bg-[#25D366]`) em vez de `bg-success` — mais reconhecível e não depende de um token pensado para outro uso.

- [ ] **Step 2: Renderizar no `StoreExperience`**

Adicionar o import e renderizar `<WhatsappFab />` junto de `<CartFab />` (mesmo nível, fora da árvore com scroll). A posição `bottom-20` (Step 1) assume que o `CartFab` ocupa até `bottom-4` mais sua própria altura — ajustar o valor se, na verificação visual do Step 3, os dois botões colidirem ou ficarem com espaçamento desigual.

- [ ] **Step 3: Verificar visualmente**

Run: `pnpm dev` — WhatsApp sempre visível (independe do carrinho), empilhado acima do carrinho quando o carrinho tem itens, sem sobreposição.

- [ ] **Step 4: Commit**

```bash
git add features/menu/components/whatsapp-fab.tsx features/menu/components/store-experience.tsx
git commit -m "feat(menu): botao flutuante do whatsapp"
```

---

## Fase D — Hero em vídeo

### Task D1: Ligar o vídeo institucional real

**Files:**
- Modify: `features/menu/media.ts:33-38`

**Interfaces:**
- Consumes: `public/brand/video_teste.mp4` (já existe no repo, fornecido como referência pelo lojista).

- [ ] **Step 1: Preencher `HERO_MEDIA`**

Trocar (linhas 33-38):

```ts
export const HERO_MEDIA: HeroMediaConfig = {
  sources: [],
  poster: null,
  posterMobile: null,
  overlayOpacity: 0.35,
};
```

por:

```ts
export const HERO_MEDIA: HeroMediaConfig = {
  sources: [{ src: "/brand/video_teste.mp4", type: "video/mp4" }],
  poster: null,
  posterMobile: null,
  overlayOpacity: 0.45,
};
```

(`overlayOpacity` sobe de `0.35` para `0.45`: o texto do Hero precisa de mais contraste sobre vídeo em movimento do que sobre um pôster estático — ajustar durante a verificação visual do Step 2 se ainda estiver ilegível ou muito escuro.)

- [ ] **Step 2: Verificar visualmente**

Run: `pnpm dev` — Home mostra o hero full-bleed cinematográfico (já é o comportamento de `cinematic = hasHeroMedia(HERO_MEDIA)` em `store-hero.tsx`, que passa a ser `true`): vídeo em autoplay, mudo, em loop, texto legível sobre o overlay. Testar também com `prefers-reduced-motion` ativado no sistema — deve cair no placeholder gráfico (`state === "placeholder"`, já que não há `poster`), não travar nem mostrar tela em branco.

- [ ] **Step 3: Checar peso da página**

Run: `pnpm build`

Confirmar que o build não tenta processar/otimizar o `.mp4` (arquivos em `public/` são servidos estáticos, sem transformação do Next) e que não há erro de tipo (`HeroVideoSource[]` já é o tipo esperado).

- [ ] **Step 4: Commit**

```bash
git add features/menu/media.ts
git commit -m "feat(menu): liga o video institucional no hero"
```

---

## Fase E — Mascote interativo

### Task E1: Recortar as 4 poses usadas nesta sprint

**Files:**
- Modify: `scripts/cutout-mascot.mjs`

**Interfaces:**
- Consumes: `removeWhiteBackground` (já definida no próprio arquivo, Task A2).
- Produces: `public/brand/mascote-pose-holding-burger.png`, `public/brand/mascote-pose-pointing-up.png`, `public/brand/mascote-pose-resting.png`, `public/brand/mascote-pose-welcoming.png` — consumidos por `mascot-poses.ts` (Task E2).

`public/brand/Versoes_boneco.png` é uma folha 1536×1024 com 8 poses em grade 4×2 (cada célula 384×512). Mapeamento usado (linha superior, esquerda→direita: braços cruzados neutro, positivo/polegar, "ok", mãos abertas; linha inferior: apontando pra cima, segurando bandeja com hambúrguer, braços cruzados recostado, espátula):

| Pose                | Coluna | Linha | Uso                          |
|----------------------|:------:|:-----:|-------------------------------|
| segurando hambúrguer |   1    |   1   | carrinho vazio                |
| apontando pra cima   |   0    |   1   | banner de promoções           |
| recostado/relaxado   |   2    |   1   | loja fechada                  |
| mãos abertas         |   3    |   0   | Sobre Nós                     |

- [ ] **Step 1: Adicionar a extração de poses ao script**

Em `scripts/cutout-mascot.mjs`, adicionar, antes de `await main();`:

```js
const POSE_SHEET_COLS = 4;
const POSE_SHEET_ROWS = 2;

/** Recorta uma célula da folha de poses (`Versoes_boneco.png`), remove o fundo branco e salva. */
async function cutoutPose({ col, row, outputName }) {
  const sheetPath = join(root, "public/brand/Versoes_boneco.png");
  const { width: sheetWidth, height: sheetHeight } =
    await sharp(sheetPath).metadata();
  const cellWidth = sheetWidth / POSE_SHEET_COLS;
  const cellHeight = sheetHeight / POSE_SHEET_ROWS;

  const cellBuffer = await sharp(sheetPath)
    .extract({
      left: Math.round(col * cellWidth),
      top: Math.round(row * cellHeight),
      width: Math.round(cellWidth),
      height: Math.round(cellHeight),
    })
    .toBuffer();

  const { data, info } = await sharp(cellBuffer)
    .ensureAlpha()
    .raw()
    .toBuffer({ resolveWithObject: true });

  const rgba = Buffer.from(data);
  for (let i = 0; i < rgba.length; i += 4) {
    const distance = distanceToWhite(rgba[i], rgba[i + 1], rgba[i + 2]);
    rgba[i + 3] = alphaFromDistance(
      distance,
      INNER_THRESHOLD,
      OUTER_THRESHOLD,
    );
  }

  const cutoutBuffer = await sharp(rgba, {
    raw: { width: info.width, height: info.height, channels: 4 },
  })
    .png({ compressionLevel: 9 })
    .toBuffer();

  const trimmed = await sharp(cutoutBuffer).trim().resize({ width: 600 }).png({ compressionLevel: 9 }).toBuffer();

  await write(`public/brand/${outputName}.png`, trimmed);
}
```

E, dentro de `main()`, depois do bloco que gera `mascote-avatar.png`:

```js
  await cutoutPose({ col: 1, row: 1, outputName: "mascote-pose-holding-burger" });
  await cutoutPose({ col: 0, row: 1, outputName: "mascote-pose-pointing-up" });
  await cutoutPose({ col: 2, row: 1, outputName: "mascote-pose-resting" });
  await cutoutPose({ col: 3, row: 0, outputName: "mascote-pose-welcoming" });
```

- [ ] **Step 2: Rodar o script**

Run: `node scripts/cutout-mascot.mjs`
Expected: os 4 novos arquivos listados acima são impressos e passam a existir em `public/brand/`.

- [ ] **Step 3: Verificar visualmente cada pose**

Abrir os 4 PNGs — confirmar fundo transparente e que cada um corresponde à pose esperada pela tabela do Step 1. Se o recorte de alguma célula cortar a mão/objeto que sai do quadro da grade (personagem tocando a borda da célula), ajustar `cellWidth`/`cellHeight` com uma margem manual (ex. estender `width`/`height` em alguns pixels e usar `left`/`top` negativos com `sharp`'s `extend` antes do `extract`) — só se a verificação visual mostrar corte real.

- [ ] **Step 4: Commit**

```bash
git add scripts/cutout-mascot.mjs public/brand/mascote-pose-*.png
git commit -m "feat(brand): recorta as 4 poses do mascote usadas na sprint"
```

---

### Task E2: Componente `MascotMoment` e registro de poses

**Files:**
- Create: `features/menu/mascot-poses.ts`
- Create: `features/menu/components/mascot-moment.tsx`

**Interfaces:**
- Consumes: os 4 PNGs da Task E1.
- Produces: `MASCOT_POSES: Record<MascotPoseName, { src: string; alt: string }>`, `<MascotMoment pose={MascotPoseName} message={string} />` — consumido pelas Tasks E3–E6.

- [ ] **Step 1: Escrever o registro de poses**

```ts
// features/menu/mascot-poses.ts

/**
 * Poses do mascote "Gordinho" recortadas por `scripts/cutout-mascot.mjs` a
 * partir de `public/brand/Versoes_boneco.png`. Uma entrada por pose
 * realmente usada — adicionar uma nova exige gerar o PNG primeiro (ver
 * Task E1 do plano da Sprint 8.1).
 */
export type MascotPoseName =
  | "holding-burger"
  | "pointing-up"
  | "resting"
  | "welcoming";

export const MASCOT_POSES: Record<
  MascotPoseName,
  { src: string; alt: string }
> = {
  "holding-burger": {
    src: "/brand/mascote-pose-holding-burger.png",
    alt: "Gordinho segurando um hambúrguer",
  },
  "pointing-up": {
    src: "/brand/mascote-pose-pointing-up.png",
    alt: "Gordinho apontando para cima",
  },
  resting: {
    src: "/brand/mascote-pose-resting.png",
    alt: "Gordinho descansando",
  },
  welcoming: {
    src: "/brand/mascote-pose-welcoming.png",
    alt: "Gordinho de braços abertos, dando as boas-vindas",
  },
};
```

- [ ] **Step 2: Escrever o componente**

```tsx
// features/menu/components/mascot-moment.tsx
import Image from "next/image";

import { MASCOT_POSES, type MascotPoseName } from "@/features/menu/mascot-poses";
import { cn } from "@/lib/utils";

/**
 * Momento do mascote: uma pose do Gordinho + uma mensagem curta, para os
 * pontos da área do cliente listados na Sprint 8.1 (carrinho vazio, loja
 * fechada, banner de promoções, Sobre Nós). Todos fora do fluxo de
 * checkout, que está fora de escopo desta sprint.
 */
export function MascotMoment({
  pose,
  message,
  className,
}: {
  pose: MascotPoseName;
  message: string;
  className?: string;
}) {
  const { src, alt } = MASCOT_POSES[pose];

  return (
    <div className={cn("flex flex-col items-center gap-3 text-center", className)}>
      <Image src={src} alt={alt} width={160} height={200} className="h-auto w-32 object-contain" />
      <p className="max-w-xs text-sm font-medium text-muted-foreground">{message}</p>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add features/menu/mascot-poses.ts features/menu/components/mascot-moment.tsx
git commit -m "feat(menu): componente MascotMoment e registro de poses"
```

---

### Task E3: Mascote no carrinho vazio

**Files:**
- Modify: `features/cart/components/cart-panel-content.tsx:20-27`

**Interfaces:**
- Consumes: `MascotMoment` (Task E2).

- [ ] **Step 1: Trocar o `EmptyState` genérico pelo `MascotMoment`**

Trocar (linhas 20-27):

```tsx
  if (items.length === 0) {
    return (
      <EmptyState
        icon={ShoppingBag}
        title="Seu carrinho está vazio"
        description="Adicione itens do cardápio para montar seu pedido."
      />
    );
  }
```

por:

```tsx
  if (items.length === 0) {
    return (
      <div className="flex flex-1 flex-col items-center justify-center gap-3 px-6 py-12">
        <MascotMoment
          pose="holding-burger"
          message="Seu carrinho está vazio — adicione itens do cardápio para montar seu pedido."
        />
      </div>
    );
  }
```

Trocar o import `import { EmptyState } from "@/components/empty-state";` por `import { MascotMoment } from "@/features/menu/components/mascot-moment";`. Remover também o import `ShoppingBag` do topo do arquivo se não for mais usado em nenhuma outra linha:

Run: `grep -n "ShoppingBag" features/cart/components/cart-panel-content.tsx`

- [ ] **Step 2: Verificar visualmente**

Run: `pnpm dev` — abrir o carrinho vazio: mostra o Gordinho segurando um hambúrguer + mensagem, no lugar do ícone genérico.

- [ ] **Step 3: Commit**

```bash
git add features/cart/components/cart-panel-content.tsx
git commit -m "feat(cart): mascote no carrinho vazio"
```

---

### Task E4: Mascote no banner de promoções

**Files:**
- Modify: `features/menu/components/store-promo-banner.tsx:1-34`

**Interfaces:**
- Consumes: `MascotMoment` (Task E2).

- [ ] **Step 1: Trocar o ícone `Sparkles` pelo mascote**

Trocar (linhas 31-34):

```tsx
      <Reveal className="mx-auto flex max-w-3xl flex-col items-center gap-4 text-center">
        <span className="flex size-14 items-center justify-center rounded-full bg-primary text-primary-foreground">
          <Sparkles className="size-7" aria-hidden />
        </span>
```

por:

```tsx
      <Reveal className="mx-auto flex max-w-3xl flex-col items-center gap-4 text-center">
        <Image
          src="/brand/mascote-pose-pointing-up.png"
          alt="Gordinho apontando para as promoções"
          width={140}
          height={175}
          className="h-auto w-28 object-contain"
        />
```

Remover o import `Sparkles` de `lucide-react` (linha 3) se não sobrar outro uso no arquivo, e adicionar `import Image from "next/image";` no topo.

- [ ] **Step 2: Verificar visualmente**

Run: `pnpm dev` — seção `#promocoes` mostra o Gordinho apontando, no lugar do ícone genérico de estrela.

- [ ] **Step 3: Commit**

```bash
git add features/menu/components/store-promo-banner.tsx
git commit -m "feat(menu): mascote no banner de promocoes"
```

---

### Task E5: Mascote quando a loja está fechada

**Files:**
- Create: `features/menu/use-store-open-state.ts`
- Modify: `features/menu/components/store-open-badge.tsx` (usar o novo hook em vez da lógica inline)
- Modify: `features/menu/components/store-hero.tsx:63-73` (bloco de badges no topo do `content`)

**Interfaces:**
- Produces: `useStoreOpenState(): { isOpen: boolean; label: string } | null` — `null` durante o snapshot de servidor/primeira renderização, mesmo significado que o `minute === null` que `StoreOpenBadge` já tratava inline.
- Consumes: `MascotMoment` (Task E2).

- [ ] **Step 1: Extrair o hook compartilhado**

`StoreOpenBadge` resolve `isOpen` internamente e não expõe esse booleano para nenhum outro componente. `StoreHero` agora precisa do mesmo booleano para decidir se mostra o Gordinho descansando — em vez de duplicar a assinatura de `useSyncExternalStore` nos dois arquivos, extrair um hook compartilhado.

Criar `features/menu/use-store-open-state.ts`:

```ts
// features/menu/use-store-open-state.ts
"use client";

import { useSyncExternalStore } from "react";

import { getStoreOpenState } from "@/features/menu/store-info";

const MINUTE_MS = 60_000;

function subscribe(onStoreChange: () => void) {
  const id = window.setInterval(onStoreChange, MINUTE_MS);
  return () => window.clearInterval(id);
}

function getMinuteSnapshot(): number {
  return Math.floor(Date.now() / MINUTE_MS);
}

function getServerSnapshot(): number | null {
  return null;
}

/**
 * Estado aberto/fechado da loja, recalculado a cada minuto
 * (`useSyncExternalStore`, convenção do projeto para APIs de tempo —
 * evita hydration mismatch). `null` no snapshot de servidor/primeira
 * renderização: quem consome decide o placeholder (badge neutro, mascote
 * ausente etc.) em vez de assumir "fechado" antes de saber a hora real.
 */
export function useStoreOpenState(): { isOpen: boolean; label: string } | null {
  const minute = useSyncExternalStore(
    subscribe,
    getMinuteSnapshot,
    getServerSnapshot,
  );

  if (minute === null) return null;
  return getStoreOpenState(new Date());
}
```

- [ ] **Step 2: Migrar `StoreOpenBadge` para o hook**

Em `features/menu/components/store-open-badge.tsx`, trocar todo o corpo do componente (que hoje chama `useSyncExternalStore` diretamente com `subscribe`/`getMinuteSnapshot`/`getServerSnapshot` locais) para consumir `useStoreOpenState()`:

```tsx
// features/menu/components/store-open-badge.tsx
"use client";

import { useStoreOpenState } from "@/features/menu/use-store-open-state";
import { cn } from "@/lib/utils";

/** Badge Aberto/Fechado da barra superior. */
export function StoreOpenBadge() {
  const state = useStoreOpenState();

  if (state === null) {
    return (
      <span
        className="h-7 w-28 animate-pulse rounded-full bg-secondary"
        aria-hidden
      />
    );
  }

  const { isOpen, label } = state;

  return (
    <span
      className={cn(
        "inline-flex shrink-0 items-center gap-1.5 rounded-full px-3 py-1.5 text-xs font-semibold whitespace-nowrap",
        isOpen ? "bg-success/15 text-success" : "bg-secondary text-muted-foreground",
      )}
    >
      <span
        className={cn(
          "size-1.5 rounded-full",
          isOpen ? "bg-success" : "bg-muted-foreground",
        )}
        aria-hidden
      />
      {label}
    </span>
  );
}
```

Remover os imports agora sem uso (`useSyncExternalStore`, `getStoreOpenState`) do topo do arquivo.

- [ ] **Step 3: Usar o hook em `StoreHero`**

No topo de `store-hero.tsx`, adicionar os imports:

```ts
import { useStoreOpenState } from "@/features/menu/use-store-open-state";
import { MascotMoment } from "@/features/menu/components/mascot-moment";
```

Dentro de `StoreHero`, antes do `const content = (`, adicionar:

```tsx
  const openState = useStoreOpenState();
  const isOpen = openState === null ? true : openState.isOpen;
```

(`isOpen` assume `true` durante o snapshot de servidor/primeira renderização — mesma decisão segurança-por-omissão que `StoreOpenBadge` já toma ao mostrar um placeholder neutro em vez de "fechado" antes de saber a hora real.)

- [ ] **Step 4: Renderizar o mascote quando fechado**

No `content`, logo depois do `</div>` que fecha o bloco de badges (linha 73), adicionar:

```tsx
      {!isOpen && (
        <MascotMoment
          pose="resting"
          message="Estamos fechados agora, mas o cardápio já está aberto — deixe seu pedido pronto para quando reabrirmos."
          className="items-start text-left"
        />
      )}
```

- [ ] **Step 5: Verificar visualmente**

Simular fechado: ajustar temporariamente o horário do sistema ou os dados de `store-info.ts` em ambiente de dev (sem alterar o arquivo de produção) para confirmar que o Gordinho descansando aparece só quando a loja está fechada, e desaparece quando aberta. Confirmar também que o badge Aberto/Fechado da `StoreOpenBadge` continua funcionando normalmente após a migração do Step 2.

- [ ] **Step 6: Validações**

Run: `pnpm build && pnpm test`
Expected: sucesso.

- [ ] **Step 7: Commit**

```bash
git add features/menu/use-store-open-state.ts features/menu/components/store-open-badge.tsx features/menu/components/store-hero.tsx
git commit -m "feat(menu): mascote descansando quando a loja esta fechada"
```

---

### Task E6: Mascote em "Sobre Nós"

**Files:**
- Modify: `features/menu/components/store-about.tsx:38-54`

**Interfaces:**
- Consumes: `MascotMoment` (Task E2).

- [ ] **Step 1: Adicionar o mascote ao bloco de texto da história**

Em `store-about.tsx`, adicionar o import `import { MascotMoment } from "@/features/menu/components/mascot-moment";` e, dentro do primeiro `<div className="flex flex-col gap-6">` (linha 38), antes do parágrafo `{ABOUT_STORY}` (linha 48-50), adicionar:

```tsx
          <MascotMoment
            pose="welcoming"
            message="Quem conta a nossa história é o Gordinho."
            className="items-start text-left"
          />
```

- [ ] **Step 2: Verificar visualmente**

Run: `pnpm dev` — seção `#sobre` mostra o Gordinho de braços abertos antes do texto da história.

- [ ] **Step 3: Commit**

```bash
git add features/menu/components/store-about.tsx
git commit -m "feat(menu): mascote na secao Sobre Nos"
```

---

## Fase F — Revisão UX final

### Task F1: Sweep de contraste e limpeza de imports órfãos

**Files:**
- Modify: qualquer arquivo sob `features/menu/`, `features/cart/`, `components/` com achados do Step 1.

- [ ] **Step 1: Buscar uso hard-coded de branco sobre `bg-primary`/`bg-accent`**

Run: `grep -rn "bg-primary" --include="*.tsx" features/ components/ | grep -i "text-white\|text-surface-dark-foreground"`

Cada ocorrência precisa trocar para `text-primary-foreground` (o token, que já é o escuro correto pós Fase A) — não há caso legítimo de texto branco fixo sobre `bg-primary` depois da ADR 0013.

- [ ] **Step 2: Buscar imports órfãos deixados pelas fases anteriores**

Run: `pnpm lint`

O ESLint do projeto já falha em import não usado (padrão Next/TypeScript) — resolver cada apontamento.

- [ ] **Step 3: Confirmar que `StoreTopbar` não deixou rastro em testes/docs**

Run: `grep -rln "StoreTopbar\|store-topbar" --include="*.ts" --include="*.tsx" --include="*.md" .`

Expected: nenhuma ocorrência fora do histórico do git (CHANGELOG antigo, se citar a criação original, não precisa ser reescrito — só código e specs/plans novos).

- [ ] **Step 4: Validação final completa**

Run: `pnpm build && pnpm tsc --noEmit && pnpm lint && pnpm test`
Expected: os quatro comandos passam sem erro.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "fix(menu): sweep de contraste e limpeza pos-Sprint-8.1"
```

---

## Documentação e encerramento (Definition of Done do projeto)

Depois da Fase F, seguir o DoD já documentado em `CLAUDE.md`:

- [ ] Atualizar `docs/frontend.md` com a nova estrutura de navegação (4 itens) e os novos componentes (`MascotNavBubble`, `CartFab`, `WhatsappFab`, `MascotMoment`).
- [ ] Atualizar `CHANGELOG.md` com as 6 fases.
- [ ] Atualizar `BACKLOG.md`: registrar a exceção de Combos (Task B5, se aplicável), e os dois momentos do mascote fora de escopo (pedido enviado, erro de conexão — ligados ao checkout).
- [ ] Reconstruir o Graphify (build completo, não `--update`, por já ser regra do projeto para mudanças estruturais).
- [ ] Sprint Report final.

---

## Self-Review

**Cobertura da spec:** paleta (Fase A) ✅, mascote como identidade/botão Home (Fase B, Task B2) ✅, remoção da topbar (Fase B, Task B3) ✅, carrinho flutuante (Fase C, Task C1) ✅, WhatsApp flutuante (Fase C, Task C2) ✅, sidebar simplificada (Fase B, Task B5) ✅, hero em vídeo (Fase D) ✅, mascote interativo nos 4 pontos fora do checkout (Fase E) ✅, revisão UX final (Fase F) ✅. "Pedido enviado"/"erro de conexão" ficam documentados como fora de escopo (checkout) desde a spec, sem task correspondente — intencional.

**Placeholders:** nenhum "TBD"/"implementar depois" — a única ambiguidade genuína (Combos sem dado real de badge) tem uma decisão condicional explícita na Task B5, Step 1, não um placeholder.

**Consistência de tipos:** `MascotPoseName` definido em `mascot-poses.ts` (Task E2) é o único tipo usado por `MascotMoment` e por todas as Tasks E3–E6 (`"holding-burger"`, `"pointing-up"`, `"resting"`, `"welcoming"`) — confere com os 4 arquivos gerados na Task E1.
